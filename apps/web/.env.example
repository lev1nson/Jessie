# ===== REQUIRED ENVIRONMENT VARIABLES =====
# Copy this file to .env.local and fill in your actual values

# Supabase Configuration (from Supabase Dashboard → Settings → API)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...
SUPABASE_SERVICE_ROLE_KEY=eyJhbGciOi...

# Google OAuth Configuration (from Google Cloud Console → Credentials)
GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=GOCSPX-your-secret

# OpenAI Configuration (from OpenAI Dashboard → API Keys)
OPENAI_API_KEY=sk-proj-your-key

# NextAuth Configuration (generate with: openssl rand -hex 32)
NEXTAUTH_SECRET=your-32-char-hex-string
NEXTAUTH_URL=http://localhost:3000

# Cron Job Security (generate with: openssl rand -hex 32)
CRON_SECRET=your-secure-cron-secret

# ===== OPTIONAL CONFIGURATION =====

# Development Settings
LOG_LEVEL=debug
SKIP_EMAIL_SYNC=false
MOCK_OPENAI_API=false

# Performance Tuning
GMAIL_SYNC_INTERVAL=3600
MAX_EMAILS_PER_SYNC=100
VECTOR_SEARCH_LIMIT=10
OPENAI_MAX_TOKENS=2000

# ===== HOW TO GET EACH VARIABLE =====
#
# 1. SUPABASE VARIABLES:
#    - Go to: https://supabase.com/dashboard/project/YOUR_PROJECT
#    - Settings → API → Copy the URL and keys
#
# 2. GOOGLE OAUTH:
#    - Go to: https://console.cloud.google.com/apis/credentials
#    - Create OAuth 2.0 Client ID → Web application
#    - Authorized redirect URIs: http://localhost:3000/auth/callback
#
# 3. OPENAI API KEY:
#    - Go to: https://platform.openai.com/api-keys
#    - Create new secret key
#
# 4. GENERATE SECRETS:
#    - Run: openssl rand -hex 32
#    - Use output for NEXTAUTH_SECRET and CRON_SECRET